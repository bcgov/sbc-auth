<template>
  <v-dialog v-model="isDialogOpen" max-width="1140">
    <v-card class="d-flex">
      <!-- Left Panel -->
      <div class="col-4 p-5">
        <p>AUTHENTICATION OPTIONS</p>
        <h2>
          <v-icon large color="primary" class="mr-4">mdi-two-factor-authentication</v-icon><span>BCeID and 2-factor authentication app</span>
        </h2>
        <div class="my-3">
          <ul>
            <li>Uses your BCeID and a 2-factor authentication app that generates a verification code on your phone or in your browser</li>
            <li>Whenever you log in you will need a username and password like normal.</li>
            <li>In addition to your password, you’ll also need a code generated by the Google Authenticator app on your phone or through a browser plugin.</li>
          </ul>
        </div>
        <div class="account-type__buttons">
          <v-btn
            large
            depressed
            block
            color="primary"
            class="font-weight-bold mb-3"
            @click="select()"
          >
            SELECT
          </v-btn>
          <v-btn
            large
            depressed
            block
            color="primary"
            class="font-weight-bold"
            outlined
            @click="close()"
          >
            More Options
          </v-btn>
        </div>
      </div>

      <v-divider vertical />

      <!-- Right Panel -->
      <div class="col-7">
        <h2 class="mt-9">How It Works</h2>
      </div>
      <v-btn class="mt-3 ml-3" icon @click="close()"><v-icon>mdi-close</v-icon></v-btn>
    </v-card>
  </v-dialog>
</template>

<script lang="ts">
import { Component, Emit, Vue } from 'vue-property-decorator'

@Component({})
export default class LearnMoreBCEID extends Vue {
  private isDialogOpen = false

  public open () {
    this.isDialogOpen = true
  }

  public close () {
    this.isDialogOpen = false
  }

  @Emit('bceid-selected')
  private select () {
    this.close()
  }
}
</script>

<style lang="scss" scoped>
  li {
    margin-top: 3em;
    margin-bottom: 3em;
  }
</style>
