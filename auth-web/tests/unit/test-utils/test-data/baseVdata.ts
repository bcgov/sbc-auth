const header = '.base-table__header'
const headerFilters = `${header}__filter`
export const baseVdataTable = {
  'header': header,
  'headerTitles': `${header}__title`,
  'headerFilters': headerFilters,
  'headerFilterSelects': `${headerFilters}__select`,
  'headerFilterTextbox': `${headerFilters}__textbox`,
  'itemRow': '.base-table__item-row',
  'itemCell': '.base-table__item-cell'
}
