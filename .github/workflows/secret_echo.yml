
name: Auth Web DEV CD

on:
  push:
    branches:
      - gha
jobs:
  build:    
      runs-on: ubuntu-latest

      strategy:
          matrix:
            node-version: [12.x]
      steps:  
        - name: Print Secrets
          run: echo "${{ secrets.SECRET_MSG }}"
   
